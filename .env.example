# Orion server configuration
# Copy to .env and fill in (or set as environment variables).
# The app also works out of the box with the default URI below.

MONGO_URI=mongodb+srv://willythedev:poAAAi6hLZ55cEm6@cluster0.1umip.mongodb.net/?appName=Cluster0
DB_NAME=orion
PORT=3000

# Optional: bootstrap the first admin automatically on startup
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=change-me
# ADMIN_EMAIL=admin@example.com
