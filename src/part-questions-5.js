/* Orion question bank — part 5: complex, advanced questions with code snippets */
  { id: 'c01', cat: 'system-design', diff: 'advanced', type: 'mcq',
    q: 'Your checkout API retries a payment request when the gateway times out. The customer was charged, but the retry charges them again. Which mechanism prevents this?',
    options: [
      'A distributed lock on the payment gateway',
      'An idempotency key sent with each retry',
      'A longer client timeout',
      'Circuit breaking the gateway'
    ],
    answer: 1,
    explain: 'An idempotency key lets the gateway recognize a retry of the same logical request and return the original result instead of charging twice.' },
  { id: 'c02', cat: 'system-design', diff: 'advanced', type: 'fill',
    q: 'The ___ pattern persists an event in the same database transaction as the business change, so a worker can reliably publish it later.',
    answer: ['outbox', 'transactional outbox', 'outbox pattern'],
    explain: 'The transactional outbox guarantees the event is never lost: it commits with the business write, and a relay publishes it to the broker.' },
  { id: 'c03', cat: 'system-design', diff: 'advanced', type: 'mcq',
    q: 'A leader election (e.g. Raft) exists in your distributed system. Its purpose is to:',
    options: [
      'Distribute load evenly across replicas',
      'Ensure a single authoritative node coordinates writes',
      'Encrypt inter-node communication',
      'Cache frequently read data'
    ],
    answer: 1,
    explain: 'Leader election guarantees a single coordinator at a time, which avoids split-brain conflicting writes while allowing failover.' },
  { id: 'c04', cat: 'system-design', diff: 'advanced', type: 'mcq',
    q: 'Your system must never lose an order event, but consumers can tolerate a few seconds of delay. Which queue is most appropriate?',
    options: [
      'In-memory queue in the API process',
      'A persistent message broker with at-least-once delivery',
      'A UDP broadcast',
      'An HTTP callback with no retry'
    ],
    answer: 1,
    explain: 'A persistent broker (e.g. Kafka, SQS) survives restarts and delivers at-least-once — exactly the durability/latency trade-off described.' },
  { id: 'c05', cat: 'frontend', diff: 'advanced', type: 'mcq',
    q: 'Why does this effect keep firing on every render?',
    code: 'function App() {\n  const [count, setCount] = useState(0);\n  useEffect(() => {\n    console.log("ran", count);\n  }, [count]);\n  return <button onClick={() => setCount(c => c + 1)}>\n    {count}\n  </button>;\n}',
    options: [
      'The dependency array is missing count',
      'The effect runs on every render by design; this is correct behaviour',
      'useState is being called after useEffect',
      'Effects always re-run when the component re-renders'
    ],
    answer: 1,
    explain: 'Effects re-run whenever their dependencies change. count changes with each click, so it logging every render is correct, not a bug.' },
  { id: 'c06', cat: 'frontend', diff: 'advanced', type: 'mcq',
    q: 'With Next.js Incremental Static Regeneration (ISR), what happens after a page with revalidate: 60 is requested?',
    options: [
      'The page is regenerated on every request',
      'The cached page is served for up to 60s; a background rebuild refreshes it afterward',
      'The page is only built once and never changes',
      'The page becomes fully client-rendered'
    ],
    answer: 1,
    explain: 'ISR serves the stale page for the revalidate window, then triggers a background regeneration so the next request gets fresh content.' },
  { id: 'c07', cat: 'frontend', diff: 'advanced', type: 'fill',
    q: 'The process of attaching client-side event handlers and state to server-rendered HTML is called ___.',
    answer: ['hydration', 'rehydration'],
    explain: 'Hydration makes server-rendered markup interactive by attaching React\u2019s runtime to the existing DOM.' },
  { id: 'c08', cat: 'frontend', diff: 'advanced', type: 'mcq',
    q: 'What does this render after clicking the button twice?',
    code: 'function Counter() {\n  const [n, setN] = useState(0);\n  return (\n    <button onClick={() => {\n      setN(n + 1);\n      setN(n + 1);\n    }}>{n}</button>\n  );\n}',
    options: ['0', '1', '2', '3'],
    answer: 1,
    explain: 'Both setN calls use the same stale n from this render, so the final state is n + 1. The functional form setN(x => x + 1) twice would yield +2.' },
  { id: 'c09', cat: 'backend', diff: 'advanced', type: 'mcq',
    q: 'What is the order of the logged values?',
    code: 'console.log("A");\nsetTimeout(() => console.log("B"), 0);\nPromise.resolve().then(() => console.log("C"));\nconsole.log("D");',
    options: ['A, D, C, B', 'A, D, B, C', 'A, C, B, D', 'D, A, B, C'],
    answer: 0,
    explain: 'Synchronous code runs first (A, D); the promise microtask (C) runs before the macrotask timeout (B) in the same tick.' },
  { id: 'c10', cat: 'backend', diff: 'advanced', type: 'fill',
    q: 'When a readable stream\u2019s consumer is slower than the producer, the mechanism that stops buffering is called ___.',
    answer: ['backpressure', 'back pressure'],
    explain: 'Backpressure pauses the producer when the consumer can\u2019t keep up, preventing unbounded memory growth.' },
  { id: 'c11', cat: 'backend', diff: 'advanced', type: 'mcq',
    q: 'Where is the best place to catch an error thrown inside an async Express route?',
    options: [
      'In a .catch() on each route handler',
      'In a dedicated error-handling middleware at the end of the chain',
      'In a try/catch inside res.json',
      'Express catches it automatically and returns 500'
    ],
    answer: 1,
    explain: 'A 4-arity error middleware centralizes handling. For async handlers, wrap them (or use an async wrapper) so rejections reach it.' },
  { id: 'c12', cat: 'db-mongodb', diff: 'advanced', type: 'mcq',
    q: 'Which aggregation pipeline returns one document per distinct category with a count?',
    code: 'db.items.aggregate([\n  // ???\n])',
    options: [
      '[ { $group: { _id: "$cat", n: { $sum: 1 } } } ]',
      '[ { $match: { cat: 1 } }, { $count: "$cat" } ]',
      '[ { $project: { cat: 1 } }, { $distinct: "cat" } ]',
      '[ { $unwind: "$cat" }, { $count: "cat" } ]'
    ],
    answer: 0,
    explain: '$group with _id: "$cat" and $sum: 1 buckets documents by category and counts each bucket — the classic group-by-count.' },
  { id: 'c13', cat: 'db-mongodb', diff: 'advanced', type: 'mcq',
    q: 'Given a compound index on { a: 1, b: -1 }, which query can use it most efficiently?',
    options: [
      'Filter on b only',
      'Filter on a only, sort by b descending',
      'Filter on b, sort by a',
      'A range query on both a and b with no sort'
    ],
    answer: 1,
    explain: 'The index leads with a, so equality on a is indexed; b\u2019s descending order matches the index direction, avoiding a sort.' },
  { id: 'c14', cat: 'db-mongodb', diff: 'advanced', type: 'fill',
    q: 'When a secondary replica promotes itself to primary after the old primary fails, the process is called ___.',
    answer: ['failover', 'automatic failover', 'election'],
    explain: 'Replica set members run an election and the majority elects a new primary — automatic failover keeps the set writable.' },
  { id: 'c15', cat: 'db-postgres', diff: 'advanced', type: 'mcq',
    q: 'Given the table and query below, which index helps most?',
    code: 'CREATE TABLE orders (id bigserial, user_id int, total numeric, status text);\n-- Frequent query:\nSELECT * FROM orders WHERE user_id = 42\nORDER BY created_at DESC LIMIT 20;',
    options: [
      'CREATE INDEX ON orders (created_at)',
      'CREATE INDEX ON orders (user_id, created_at DESC)',
      'CREATE INDEX ON orders (total)',
      'CREATE INDEX ON orders (status, user_id)'
    ],
    answer: 1,
    explain: 'A composite (user_id, created_at) index filters by user and serves the ORDER BY in index order — no separate sort step.' },
  { id: 'c16', cat: 'db-postgres', diff: 'advanced', type: 'mcq',
    q: 'What does this window function return for the rows?',
    code: 'SELECT name, score,\n       DENSE_RANK() OVER (ORDER BY score DESC) AS r\nFROM players;',
    options: [
      'Sequential ranks with gaps after ties',
      'Sequential ranks with no gaps after ties',
      'A running total of scores',
      'The average score per player'
    ],
    answer: 1,
    explain: 'DENSE_RANK assigns consecutive ranks — tied scores share a rank but the next rank has no gap, unlike RANK().' },
  { id: 'c17', cat: 'db-postgres', diff: 'advanced', type: 'fill',
    q: 'A subquery in the FROM clause that can reference columns of the tables to its left is called a ___ join.',
    answer: ['lateral', 'lateral join', 'lateral subquery'],
    explain: 'LATERAL lets a subquery correlate with preceding FROM items — great for per-row top-N queries.' },
  { id: 'c18', cat: 'coding', diff: 'advanced', type: 'mcq',
    q: 'What is the time complexity of this function?',
    code: 'function f(n) {\n  let total = 0;\n  for (let i = 1; i < n; i *= 2) total += i;\n  return total;\n}',
    options: ['O(n)', 'O(log n)', 'O(n log n)', 'O(2^n)'],
    answer: 1,
    explain: 'i doubles each iteration, so the loop runs about log\u2082(n) times — O(log n).' },
  { id: 'c19', cat: 'coding', diff: 'advanced', type: 'mcq',
    q: 'A debounced function (delay 300ms) is called 10 times rapidly. How many times does the underlying function actually run?',
    options: ['10', '0', '1', '300'],
    answer: 2,
    explain: 'Debouncing resets the timer on every call, so the function runs once, 300ms after the last call — ideal for search inputs.' },
  { id: 'c20', cat: 'coding', diff: 'advanced', type: 'fill',
    q: 'A function with no side effects that returns the same result for the same inputs is said to be ___.',
    answer: ['pure', 'a pure function', 'pure function', 'referentially transparent', 'deterministic'],
    explain: 'Pure functions depend only on their arguments and have no observable side effects — easy to test and reason about.' },
  { id: 'c21', cat: 'coding', diff: 'advanced', type: 'mcq',
    q: 'Which option best describes the benefit of immutable data structures?',
    options: [
      'They are faster to mutate than arrays',
      'They prevent accidental state changes and simplify change detection',
      'They require less memory than mutable ones',
      'They automatically persist to disk'
    ],
    answer: 1,
    explain: 'Immutability guarantees no shared state is corrupted, and reference equality makes change detection trivial.' },
  { id: 'c22', cat: 'devops', diff: 'advanced', type: 'mcq',
    q: 'What is the main purpose of a multi-stage Docker build?',
    options: [
      'To run builds in parallel on multiple machines',
      'To keep the final image small by discarding build-time dependencies',
      'To store multiple versions of an image',
      'To reduce CPU usage during build'
    ],
    answer: 1,
    explain: 'Multi-stage builds copy only the runtime artifacts into the final stage, dropping compilers and dev dependencies.' },
  { id: 'c23', cat: 'devops', diff: 'advanced', type: 'mcq',
    q: 'A pod is up and serving traffic but not yet ready to receive requests (still warming caches). Which probe should fail?',
    options: ['Liveness probe', 'Readiness probe', 'Startup probe', 'None — use a resource limit instead'],
    answer: 1,
    explain: 'Readiness gates traffic: while it fails, the pod is kept out of the Service endpoints. Liveness restarts unhealthy pods.' },
  { id: 'c24', cat: 'devops', diff: 'advanced', type: 'fill',
    q: 'A sidecar-based layer that handles service-to-service traffic, mTLS, and observability in Kubernetes is called a service ___.',
    answer: ['mesh', 'service mesh'],
    explain: 'Service meshes (Istio, Linkerd) inject sidecars that intercept traffic to add encryption, retries, and telemetry.' },
  { id: 'c25', cat: 'solutions', diff: 'advanced', type: 'mcq',
    q: 'Your analytics pipeline must handle 50k events/sec with spikes and consumers that lag. Which architecture fits best?',
    options: [
      'Synchronous REST calls to a monolithic API',
      'An event bus (e.g. Kafka/Kinesis) with decoupled consumers',
      'A single shared database table written by all services',
      'Batch nightly ETL only'
    ],
    answer: 1,
    explain: 'Event streaming decouples producers from consumers, absorbs spikes, and replays when consumers lag — ideal at this scale.' },
  { id: 'c26', cat: 'solutions', diff: 'advanced', type: 'mcq',
    q: 'Per the Twelve-Factor App, how should configuration be managed?',
    options: [
      'Hardcoded in the application source',
      'Stored in environment variables',
      'Committed to a config file in the repo',
      'In a global constant file'
    ],
    answer: 1,
    explain: 'Twelve-factor config lives in environment variables — environment-specific values stay out of the codebase entirely.' },
  { id: 'c27', cat: 'solutions', diff: 'advanced', type: 'mcq',
    q: 'A serverless function\u2019s first invocation after a period of inactivity is slower due to ___.',
    options: [
      'Network throttling',
      'Cold starts',
      'DNS propagation',
      'Connection pooling limits'
    ],
    answer: 1,
    explain: 'Cold starts provision a fresh runtime container, adding latency until it is reused for subsequent warm invocations.' },
